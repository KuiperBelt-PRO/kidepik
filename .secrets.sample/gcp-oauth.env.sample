# Plantilla — copiar a kidepik/.secrets/gcp-oauth.env (gitignored)
# Obtener valores del JSON OAuth en consola GCP (cliente Desktop o Web + localhost).

GCP_OAUTH_CLIENT_ID=TU_CLIENT_ID.apps.googleusercontent.com
GCP_OAUTH_CLIENT_SECRET=TU_CLIENT_SECRET
GCP_PROJECT_ID=kidepik
