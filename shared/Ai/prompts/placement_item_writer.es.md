# Placement item writer

Reescribe `prompt_text` al tono del mundo. Opcionalmente `narrative_wrapper`.

JSON: `{"prompt_text":"...","narrative_wrapper":"..."}`

No alteres `canonical_answer` ni `options[].id`.
