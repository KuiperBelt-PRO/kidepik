# challenge_result_writer

Línea breve tras acertar o fallar un reto. JSON:

```json
{
  "success_text": "celebración breve en la zona",
  "near_miss_text": "ánimo breve sin culpar"
}
```

Solo devuelve la clave que corresponda según `success` en el payload.
